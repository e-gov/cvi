import { Meta, Story } from '@storybook/addon-docs';
import assetsPkg from '/libs/assets/package.json';
import uiPkg from '/libs/ui/package.json'

<Meta title="Installation" parameters={{ viewMode: 'docs' }} />

# Installation

## Veera Styles library

<Story name="Veera Styles library">
  {{
    props: { assetsPkg },
    template: `
      Latest version: {{ assetsPkg.version }}
    `,
  }}
</Story>

### Installation

1. Install the library
```
npm i --save @ria/veera-styles;
```

2. Include styles in the project

`@use` or `@forward` the styles into your stylesheet (install Dart Sass):

```
@use '~@ria/veera-styles';
```

3. Done, enjoy!

## Angular Component library

<Story name="Angular Component library">
  {{
    props: { uiPkg },
    template: `
      Latest version: {{ uiPkg.version }}
    `,
  }}
</Story>

### Angular component library peerDependencies

```
"@angular/animations": "^14.1.1",
"@angular/cdk": "^14.2.0",
"@angular/common": "^14.1.1",
"@angular/core": "^14.1.1",
"@angular/elements": "^14.1.3",
"@angular/platform-browser": "^14.1.1",
"@angular/platform-browser-dynamic": "^14.1.1"
```

### Installation

1. Install the library
```
npm i --save @ria/veera-ng;
```

2. Import UiModule in your app.module.ts.

```
import { UiModule } from '@ria/veera-ng';

@NgModule({
  ...
  imports: [BrowserModule, UiModule],
  ...
})
```

3. Done, enjoy!
